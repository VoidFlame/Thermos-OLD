--- eclipse/Clean/src/main/java/org/bukkit/command/defaults/DeopCommand.java	2019-01-11 20:13:53.089770100 -0500
+++ eclipse/Cauldron/src/main/java/org/bukkit/command/defaults/DeopCommand.java	2019-01-11 20:13:26.914629000 -0500
@@ -49,9 +49,9 @@
 
         if (args.length == 1) {
             List<String> completions = new ArrayList<String>();
-            for (OfflinePlayer player : Bukkit.getOfflinePlayers()) {
+            for (OfflinePlayer player : Bukkit.getOperators()) {
                 String playerName = player.getName();
-                if (player.isOp() && StringUtil.startsWithIgnoreCase(playerName, args[0])) {
+                if (StringUtil.startsWithIgnoreCase(playerName, args[0])) {
                     completions.add(playerName);
                 }
             }
